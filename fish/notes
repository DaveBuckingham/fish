TODO:

global class:
last search path
disable/enable format

cant plot for very long...

segfault on exit with python3

when loading file, check for correctness

slightly different float rounding for python2 and 3

fix trigger

fix behavior on "rx failed"

detect mega (or clock speed) and adjust accordingly?

make sure num_imus doesn't change for one dataset

clean up imports

better default save directory

still need arduino to stop immedietly on trigger

ASA
disable record button during test   ( DIFFICULT )
text window breaks when it gets too full
tests:
    imus self-test
    magnetometer

MAYBE:
auto-detect mag
bigger record button





NEED TO INCLUDE IN DOCUMENTATION:
random trigger values if unplugged.
